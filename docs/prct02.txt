Diferencia entre compilacion e interpretacion

Para que un programa pueda ser ejecutado por la máquina ésta debe entenderlo, para ello
disponemos de dos herramientas: 

- Los intérpretes, que hacen de intermediarios entre el código
fuente y la máquina y permiten la ejecución directa del código. 
- Los compiladores, que traducen el código fuente desde un lenguaje de alto nivel a lenguaje
ensamblador o máquina, para que pueda ser ejecutado cuantas veces se desee por el ordenador.


54. ¿Que permisos tiene el ﬁchero helloWorld.sh? ¿Se puede ejecutar directamente? (./helloWorld.sh)
	Tiene permisos de lectura y escritura para propietarios y grupos y solo de lectura para otros
	-rw-rw-r-- 1 maria maria   19 oct  2 16:03 helloWorld.rb
	No se puede ejecutar directamente, hay que indicarle con qué se desea ejecutar

56. ¿Cual es la principal diferencia entre el contenido del ﬁchero helloWorld.sh y helloWorld.rb?
¿Que signiﬁcado tiene la primera lınea del ﬁchero helloWorld.sh?

	El rb esta escrito en ruby y el sh está escrito en bash
	Indica la ruta del interprete que se va a usar
